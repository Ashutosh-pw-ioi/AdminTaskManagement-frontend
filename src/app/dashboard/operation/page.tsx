import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Operation Management",
};

export default function Page() {
  return (
    <div>
      <div>Operation DB</div>
    </div>
  );
}
